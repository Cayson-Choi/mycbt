Vercel 배포 상태를 확인하고 문제가 있으면 분석해줘.

1. `git status`로 커밋되지 않은 변경사항 확인
2. `git log --oneline -3`으로 최근 커밋 확인
3. Vercel에 배포된 최신 상태 확인 (빌드 에러가 있다면 로그 분석)
4. 빌드 테스트: `npx next build` 실행해서 에러 여부 확인

문제가 발견되면 원인과 해결 방법을 제시해줘.
